The XML schemas required for validation are vendored in xsd_pub and included
in source and wheel distributions.  Normal builds require no schema download.

Maintainers can deliberately update the snapshot from the repository root:

$ tools/update_xsd_pub.sh <full-upstream-commit-sha>
